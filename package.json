{
  "name": "antigravity-ask-bridge-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "oxlint packages",
    "test": "pnpm -r test"
  },
  "dependencies": {
    "antigravity-ask": "workspace:*"
  },
  "devDependencies": {
    "oxlint": "^0.16.0",
    "typescript": "^5.7.0",
    "vitest": "3.1.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
